# AppiumAutomation
Main file is <ble_log_parcer_50.py>, created to parse and retrieve ADB logs data(from physical Android device) once app session is complete to count the number success/failure BLE sessions as well as the timing from BLE start to finish
